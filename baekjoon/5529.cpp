#include <cstdio>
using namespace std;

int dp_td(vector<int> a, )

int main(){
	int M, N, K;
	scanf("%d %d %d",&M, &N, &K);

	vector<int> s(K,-1);
	vector<pair<int,int> > sw(K);
	for (int i=0; i<K; i++){
		int a,b;
		scanf("%d %d",&a,&b);
		sw[i]= make_pair(a,b);
	}


}